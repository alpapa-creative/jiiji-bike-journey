# じいじのバイク旅（公開デモ版）

ギフトゲーム事業の出品ページに載せる「実際に贈った事例」の匿名化デモ。
元プロジェクトは `02_game/じいじのバイク旅`（**元は変更禁止。修正はこのコピーで行う**）。

瀬戸内の「じいじの家」から湘南の「孫たちの家」まで100kmを走る横スクロールバイク旅ゲーム
（デモ版のため元の848kmから短縮。加速カーブ・応援メッセージ・ゴール演出も100kmに再スケール済み）。

## 起動方法

- `index.html`（→ `code_artifact.html` へリダイレクト）をブラウザで開くだけで動く（file:// 可）
- プレビューは `node dev-server.cjs` → http://localhost:8767/ （launch.json 登録済み・ポート台帳 8767）

## 画面構成

- タイトル画面: BGMモード選択（ノリノリ / しんみり）→ 出発
- ゲーム画面: タップ/クリック/Spaceでジャンプ（2段ジャンプ対応）。障害物=コーン・バリケード・カラス・イノシシ、回復=おにぎり・瀬戸内みかん・静岡茶
- リザルト: ゴール到着 / リタイア

## 設定の所在

- 画像: `code_artifact.html` 内 `IMAGE_SOURCES`（未設定時はCanvasベクター描画にフォールバック）
- BGM: 同 `BGM_SOURCES` / `BGM_VOLUME`
- その他ゲーム調整値は `code_artifact.html` 内（元プロジェクト由来のため冒頭CONFIG集約はされていない）

## 元プロジェクトからの匿名化内容（2026-07-13）

- 藤田建具店 → **じいじの家**（本人指示 2026-07-13。テキスト・Canvas描画・
  看板は `images/jiiji-house.png` からPillowの壁パッチタイルで撤去・関数名 `drawJiijiHouse`）
- ゴールの家 `images/house.png` はデザイン変更（本人指示）: 暖色系→青灰系にPillowで
  色替え（HSVで暖色ピクセルのみhue 210°へ・彩度55%に低減。樹木・窓は不変）
- 愛媛 → 瀬戸内（エリア名・愛媛みかん→瀬戸内みかん・コード内キー `ehime`→`setouchi`）
- 鎌倉 → 湘南（タイトル・応援メッセージ・キー `kamakura`→`shonan`）
- 日本語名の重複アセット（md5同一のmp3/png）と `docs/` を削除
- しまなみ海道・なにわ・駿河湾・富士山・848km等の公共地名/数値は残置（個人特定性なし）

## 仮設定にした項目

- 架空の地名「瀬戸内/湘南」は仮決め（変更容易: 上記の置換系統のみ）
- じいじ・こどもたちのイラストは本人判断で**そのまま使用でOK**（2026-07-13確認済み）
- **BGM差し替えの経緯**:
  1. 2026-07-13: 歌詞に「鎌倉まで」があるため原曲を外し、自作チップチューン
     （`bgm-nori.mp3`/`bgm-shinmiri.mp3`・`tools/make_bgm.py` で再生成可）に置換
  2. 2026-07-18: チップチューンは「チープ」と本人却下 → インスト版（`tools/make_inst.py` で
     センターキャンセル生成）を試作したが、**本人決定で原曲（歌入り）採用**に確定。
     `theme-nori.mp3`/`theme-shinmiri.mp3`（= 02_game/じいじのバイク旅 の原曲コピー）
- ⚠️ **歌詞「鎌倉まで」が通常プレイで流れる点は本人了承済み**（2026-07-18。歌い出し≈11.5秒以前・
  最速クリア≈23秒と提示のうえでの判断）。インスト化が必要になったら `tools/make_inst.py` で再生成可

## テスト

- 自動テストなし（元プロジェクト由来の単一HTML構成のまま）。動作確認はプレビューで
  タイトル→ゲーム開始→ジャンプ→（リタイア/クリア）を通しで行う
